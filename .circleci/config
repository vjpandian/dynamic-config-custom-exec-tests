version: 2.1
setup: true
orbs:
  path-filtering: vjpandian/path-filtering-orb@0.0.1
workflows:
  setup-workflow:
    jobs:
      - path-filtering-orb/filter:
          pre-steps:
            - checkout
            - run:
                name: print some pre-step here
                command: |
                  echo "hello"
          base-revision: main
          executor: "default-machine"
          config-path: .circleci/continue-config.yml
          mapping: |
            terraform/.* tf-deploy true
            helm/.* helm-deploy true
